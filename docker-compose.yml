services:
  web:
    build: .
    expose:
      - "8000"

  nginx:
    image: nginx:1.31.3
    depends_on:
      - web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - /etc/letsencrypt:/etc/letsencrypt:ro

